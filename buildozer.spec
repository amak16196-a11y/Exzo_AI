[app]
title = Exzo AI
package.name = exzoai
package.domain = org.jaili
source.dir = .
source.include_exts = py,png,jpg,json
version = 1.0

# المكتبات المطلوبة (السر هنا)
requirements = python3, kivy, kivymd, pyttsx3, speechrecognition, certifi

orientation = portrait
fullscreen = 1
android.arch = arm64-v8a

# أيقونة التطبيق (اختياري حالياً)
# icon.filename = icon.png
